{
  "name": "mongopool",
  "version": "1.1.0",
  "description": "Connect to mongoDB and re-use the connection to benefit from mongoDB node driver's connection pooling ",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mongodb",
    "pooling",
    "connection",
    "pool"
  ],
  "author": "Imtiaz Chowdhury",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^6.2.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
